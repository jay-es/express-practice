// JSDoc 型定義ファイル

/**
 * RequestHandler の async バージョン
 * @callback AsyncRequestHandler
 * @param {import('express').Request} Request
 * @param {import('express').Response} Response
 * @param {import('express').NextFunction} NextFunction
 * @returns {Promise<any>}
 */

// 何か export しないと他のファイルで型定義が読み込めない
module.exports = 0;
